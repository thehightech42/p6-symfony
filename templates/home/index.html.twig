{% extends 'base.html.twig' %}

{% block stylesheet %}
    <link rel="stylesheet" href="{{asset("CSS/figures.css")}}">
{% endblock %}

{% block body %}
    <div class="row rowHome mt-5 mb-5">
        <div class="col-lg-12 text-center">
            <h1>Bienvenue sur SnowTricks</h1>
            <h3>Le site spécialisés sur les figures de Snowboards</h3>
        </div>
    </div>
    <div class="row w-75 m-auto justify-content-center mt-5">
    <div class="row"  id="figures">
    
    </div>
    <div class="row">
        <div class="col-lg-12 m-auto">
        <button id="seeMoreFigure" class="btn btn-primary">Voir plus</button>
        </div>
    </div>
        {# <div class="row w-80 m-auto"> #}

            {# {% for figure in figures %}

                <div class="col-lg-3 elementsFigure hiddenHome">
                    <div class="card text-white bg-dark mb-3">
                        <div class="card-header">{{figure.title}}</div>
                        <div class="card-body">
                            <img src="http://placehold.it/280x120" alt="">
                            <h5 class="card-title"><a href="{{path('figure', {'id': figure.id }) }}">{{figure.title}}</a></h5>
                            <p class="card-text">{{figure.shortDescription | raw }}</p>
                            <p>Publié le {{figure.createAt | date('d/m/Y')}} dans le groupe "{{figure.groupe.title}}"</p>
                        </div>
                    </div>    
                </div>
        
            {% endfor %} #}

        {# </div> #}
    </div>
    

{% endblock %}
{% block javascripts %}
    {# <script src="{{ asset("js/seemore.js") }}"></script> #}
    <script src="{{ asset("/JS/AjaxFigure.js") }}"></script>
    <script>
        {# let seeMome = new Seemore("elementsFigure", 8, "seeMoreButton"); #}
        let ajaxFigure = new AjaxFigure('https://p6-symfony.numeriquesimple.fr/figure/ajax/figure' ,'seeMoreFigure');
    </script>
{% endblock %}
